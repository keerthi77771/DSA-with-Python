# INSERTION SORT. Time complexity: Best case - O(n); Average case - O(n^2); Worst case - O(n^2), Space Complexity: O(1)
def insertion_sort(arr):
  for i in range(1, len(arr)):
    j=i
    while j>0 and arr[j-1]>arr[j]:
      arr[j-1],arr[j]=arr[j],arr[j-1]
      j-=1
arr=[]
insertion_sort(arr)
print(arr)
